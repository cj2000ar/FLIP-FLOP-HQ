[Unit]
Description=FlipFlop API Daily Backup Timer
Requires=flipflop-backup.service

[Timer]
# Run every day at 2:00 AM UTC
OnCalendar=*-*-* 02:00:00
# Run 5 minutes after boot if missed
Persistent=true
RandomizedDelaySec=5min

[Install]
WantedBy=timers.target
